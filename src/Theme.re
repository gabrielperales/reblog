open Css;

let maxWidth = px(960);
let navbarHeight = px(80);

// Fonts
let primaryFontFamily = "'Raleway', sans-serif";

// Colors
let primaryColor = hsl(240, 100, 70);
let primaryLighterColor= hsl(240, 100, 80);
let primaryLightestColor= hsl(240, 100, 99);
let primaryDarkerColor= hsl(240, 100, 50);


// Responsive Break Points
let mobile = "480px";
let tablet = "756px";
let desktop = "960px";

